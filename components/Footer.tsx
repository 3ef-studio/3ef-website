export default function Footer() {
  return (
    <footer className="text-foreground text-sm">
      {/* links can be muted if you prefer: text-muted-foreground hover:text-foreground */}
      …
    </footer>
  );
}
